// IMPORTAÇÃO DA BIBLIOTECA PADRÃO PARA O USO DE TECLADO E MONITOR (Ctrl + /)
#include <stdio>

int main(){
   
    /* Meu Primeiro Código em linguagem C */

    printf ("Hello, World! \n");

// Para exibir uma mensagem na tela iremos usar o comando printf(exibir formatando)//

    return 0;

// informar ao compilador que o programa acabou (return) 
}
