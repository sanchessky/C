#include <stdio.h>
int main() {
    int v = 1;       //PODE ESOLHER OUTRO VALOR, A CONTAGEM SERA A PARTIR DO NUMERO INICIAL 
        while (v <=10){
        printf("%d\n",v);
        v++;   /* não ser infito o looping */
        }

return 0;
        


}